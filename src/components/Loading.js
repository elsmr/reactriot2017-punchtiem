import React from 'react';

const Loading = () =>
  <div
    style={{
      display: 'flex',
      justifyContent: 'center',
      alignItems: 'center',
      height: '100%',
    }}
  >
    <strong>Loading</strong>
  </div>;

export default Loading;
